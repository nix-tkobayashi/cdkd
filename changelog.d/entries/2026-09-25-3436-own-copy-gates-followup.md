- **Three more pasteable `cdkd` commands leave their own gates, the SSM import refusal's `--resource` fragment is gated, and every touched message is pasted under bash (issue [#3436](https://github.com/go-to-k/cdkd/issues/3436))** -- `src/cli/commands/export.ts`, `src/cli/commands/gc.ts`, `src/state/malformed-resources-bag.ts`, `src/deployment/rollback-executor.ts`, `src/provisioning/providers/ssm-parameter-provider.ts`, `src/utils/pasteable-command.ts`. Follow-up to [#3613](https://github.com/go-to-k/cdkd/pull/3613); the `buildImportPlan` half landed in [#3763](https://github.com/go-to-k/cdkd/pull/3763). `gc.ts`, `export.ts`'s `cdkd state show` line and `malformed-resources-bag.ts`'s `inspectCommand` build through the shared gate. At the last two, with `plainIdent` on the stack name AND the region, an altered, capped, empty, option-shaped or non-plain value prints as a quoted hole, never a sanitized or shell-quoted spelling, and the region keeps its 128 display cap through the gate's new `maxCodePoints`; an absent region still omits `--stack-region`, and `inspectCommand`'s no-name arm still returns its two-hole template. `gc.ts` keeps its outer `isPasteableIdent` gate and the default cap, withholding the whole line. `cdkd export`'s drift-gap report prints `cdkd state refresh-observed` only when the gate named both values, says why from the gate's reason otherwise, and explains a hole in its read line before the command. `cdkd import`'s SSM refusal names the logical id in `--resource <id>=<parameterName>` only when `isPasteableIdent` admits it, quotes the placeholder and bounds the displayed id at 1152. `cdkd rollback`'s two `--orphan` remedies move onto a labelled `To orphan it:` LAST line of their own (kept per line on the `Rollback failed for` log line), lose their backtick wrappers and quote the withheld placeholder. Every touched site's test pastes its rendered message into bash at line, sentence and clause granularity (`tests/unit/utils/paste-harness.ts`).
